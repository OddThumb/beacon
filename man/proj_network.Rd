% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/use_pycbc.R
\name{proj_network}
\alias{proj_network}
\title{Project GW polarizations onto multiple detectors}
\usage{
proj_network(hp, hc, dets, ra, dec, pol, t_gps, t_ref = "H1")
}
\arguments{
\item{hp}{A \code{ts} object. Plus polarization waveform.}

\item{hc}{A \code{ts} object. Cross polarization waveform.}

\item{dets}{A character vector of detector abbreviations (e.g., \code{c("H1", "L1")}).}

\item{ra}{A numeric. Right Ascension of the source (in radians).}

\item{dec}{A numeric. Declination of the source (in radians).}

\item{pol}{A numeric. Polarization angle (in radians).}

\item{t_gps}{A numeric. GPS time of signal arrival at Earth's center.}

\item{t_ref}{A character. Reference detector for computing relative arrival times (default: \code{"H1"}). [Currently unused]}
}
\value{
A named list of projected \code{ts} objects for each detector, with the following attributes:
\describe{
  \item{\code{"tgps.net"}}{Vector of GPS arrival times per detector.}
  \item{\code{"dt_rel"}}{Matrix of pairwise relative time delays between detectors.}
  \item{\code{"tgps_remainder"}}{Rounding residuals for each detector’s delay.}
}
}
\description{
Applies antenna pattern projection to gravitational-wave polarizations (\code{hp}, \code{hc})
for a network of detectors, taking into account relative arrival times due to Earth's rotation.
}
\details{
For each detector in \code{dets}, the function computes the relative arrival time of the signal
based on its sky location. It then applies \code{\link{proj_wave}} to project the polarizations
onto each detector using the corresponding time and antenna pattern.
}
\note{
Requires Python environment with \pkg{pycbc} available via \pkg{reticulate}.
}
\examples{
\dontrun{
hp <- ts(sin(seq(0, 1, length.out = 4096)), start = 1000000000, fs = 4096)
hc <- ts(cos(seq(0, 1, length.out = 4096)), start = 1000000000, fs = 4096)
proj <- proj_network(hp, hc, dets = c("H1", "L1"), ra = 1.0, dec = -0.5, pol = 0.0, t_gps = 1126259462)
}

}
