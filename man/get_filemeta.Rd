% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/OSC.R
\name{get_filemeta}
\alias{get_filemeta}
\title{Retrieve file metadata (strain-files) from GWOSC API v2}
\usage{
get_filemeta(
  obsrun,
  detector,
  GPSstart,
  GPSend,
  sampling.freq = 4096,
  dur = 4096,
  file.format = "hdf5"
)
}
\arguments{
\item{obsrun}{Character. Observation run name (e.g. "O1").}

\item{detector}{Character vector. One or more detectors (e.g. c("H1","L1")).}

\item{GPSstart}{Integer. Start GPS time.}

\item{GPSend}{Integer. End GPS time. If omitted, defaults to GPSstart + dur.}

\item{sampling.freq}{Integer (default 4096). Sampling frequency in Hz (only
4096/16384 supported because the API expects sample-rate in kHz).}

\item{dur}{Integer (default 4096). Requested segment duration (seconds).}

\item{file.format}{Character (default "hdf5"). Desired file format.}
}
\value{
data.frame with columns: GPSstart, utc_start, detector, sample_rate_kHz,
  url, detail_url (when present), duration.
}
\description{
Query GWOSC API v2 /strain-files endpoint and return a tidy data.frame of
available files matching the requested time window, detectors, sampling
frequency, duration and format.
}
\details{
Note: this function only returns file metadata (including download URL).
It does NOT perform any duty-cycle or timeline-based filtering.
}
\examples{
\dontrun{
fm <- get_filemeta("O1", c("H1", "L1"), 1126051217, 1126051217 + 86400)
}
}
